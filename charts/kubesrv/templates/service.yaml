apiVersion: v1
kind: Service
metadata:
  name: {{ include "kubesrv.fullname" . }}
  labels:
    {{- include "kubesrv.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kubesrv.selectorLabels" . | nindent 4 }}
