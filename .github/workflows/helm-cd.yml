name: Helm CD

on:
  push:
    branches: [ master ]
    paths:
      - 'charts/**'
      - '.github/workflows/helm-cd.yml'
  pull_request:
    branches: [ master ]
    paths:
      - 'charts/**'

jobs:
  lint-and-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Helm
        uses: azure/setup-helm@v4
        with:
          version: v3.14.0

      - name: Lint Helm Chart
        run: helm lint charts/kubesrv

      - name: Template Validation (Default)
        run: helm template kubesrv charts/kubesrv --debug

      - name: Template Validation (CI Values)
        run: helm template kubesrv charts/kubesrv -f charts/kubesrv/ci/test-values.yaml --debug

      - name: Install kube-linter
        run: |
          KUBE_LINTER_VERSION=0.6.8
          KUBE_LINTER_SHA256="405103c8095f4e565987163359d9972b2a60a74b88939c4f1c1f46399b959550"
          curl -L -o kube-linter.tar.gz "https://github.com/stackrox/kube-linter/releases/download/v${KUBE_LINTER_VERSION}/kube-linter-linux.tar.gz"
          echo "${KUBE_LINTER_SHA256}  kube-linter.tar.gz" | sha256sum -c -
          tar -xzf kube-linter.tar.gz
          sudo mv kube-linter /usr/local/bin/
          rm kube-linter.tar.gz

      - name: Security Scan with kube-linter
        run: kube-linter lint charts/kubesrv

  deploy-preview:
    if: github.event_name == 'pull_request'
    needs: lint-and-test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Create KinD Cluster
        uses: helm/kind-action@v1.9.0

      - name: Deploy to KinD
        run: |
          helm install kubesrv-preview charts/kubesrv \
            --set image.tag=latest \
            --wait \
            --timeout 5m

      - name: Verify Deployment
        run: |
          kubectl get pods
          kubectl get svc kubesrv-preview-kubesrv

      - name: Run Helm Tests
        run: helm test kubesrv-preview

